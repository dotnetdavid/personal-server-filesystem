#steps:
#  - label: ":hammer: Build personal-server-filesystem"
#    commands:
#      - echo "Starting build..."
#      - pwd
#      - ls -la
#      - python --version || true
#      - node --version || true
#      - echo "Build complete."
#    agents:
#      queue: "default-self-hosted"


steps:
  - label: ":docker: Full Rebuild"
    commands:
      - make stop
      - make nuke --yes
      - make daemon
      - make status
    agents:
      queue: "default-self-hosted"
