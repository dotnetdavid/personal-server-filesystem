# env/env.mk.example — machine-specific configuration for Makefile
#
# Copy this file to env/env.mk and update the paths for your system.
# This file is included by the Makefile and provides variables for
# Docker, Python, Terraform, and project paths.
#
# NOTE:
# - Use MSYS2-style paths (/c/... or /d/...)
# - env.mk is used by Make, not by shell scripts
# - VAULT_ROOT should match env.sh

# Tool paths
DOCKER := docker

# Project paths
PROJECT_ROOT := /c/path-to-project/personal-server-filesystem
VAULT_ROOT := /c/path-to-vault/vault

# Container mount mapping (host → container)
CONTAINER_MOUNT := $(VAULT_ROOT):/personal