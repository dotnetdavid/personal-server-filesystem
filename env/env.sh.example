# env/env.sh.example — machine-specific configuration for shell scripts
#
# Copy this file to env/env.sh and update the paths for your system.
# This file is sourced by Bash scripts and Makefile targets.
#
# NOTE:
# - Use MSYS2-style paths (/c/... or /d/...)
# - env.sh is the *canonical* source of truth for VAULT_ROOT
# - `make sync-env` will generate env/env.compose automatically

# Docker CLI — usually just "docker" if it's on PATH
export DOCKER="docker"

# Project root directory
export PROJECT_ROOT="/d/devel/mcp-impls/personal-server-filesystem"

# Path to your Obsidian vault on the host machine
export VAULT_ROOT="/d/ai-projects/Telos/Personal"

# Container mount mapping (host → container)
export CONTAINER_MOUNT="$VAULT_ROOT:/personal"