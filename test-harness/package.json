{
  "name": "personal-server-filesystem-test-harness",
  "version": "1.0.0",
  "type": "module",
  "description": "Test harness for the Personal Server Filesystem MCP server",
  "private": true,
  "devDependencies": {
    "typescript": "^5.0.0",
    "vitest": "^1.0.0"
  },
  "scripts": {
    "test": "vitest",
    "test:ci": "vitest run --reporter=basic"
  }
}